// Utilizando arrow function e map.
// Criar um array de números e manipular os números desse array,
// retornando um array novo com os números do primeiro array
// multiplicado por 2

const numeros = [2, 3, 6, 5, 10]

console.log('newArray', numeros.map((numero) => numero * 2))